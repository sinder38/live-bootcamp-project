// The User struct should contain 3 fields. email, which is a String;
// password, which is also a String; and requires_2fa, which is a boolean.
pub struct User {
    todo!()
}

impl User {
    todo!() // add a constructor function called `new`
}
